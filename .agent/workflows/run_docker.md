---
description: Build and run the Docker container for the search agent
---

1. Build the Docker image:
```bash
docker-compose build
```

2. Run the agent interactively:
```bash
docker-compose run --rm search-agent
```
